<!-- coverage: timestamp=2026-08-03T09:26:55Z repo=danieljustus/symaira-corekit head=8bef3327e89b59c8e617b37dbebe86bf21cdfdc3 overall=85.8 patch=90.0 baseline=85.1 delta=+0.7 mutation=n/a assertdensity=2.52 slowtests=0 suite=n/a -->

# Coverage Review - 2026-08-03

## Summary
- Tooling: `CGO_ENABLED=0 go test -covermode=atomic -coverprofile` / `go tool cover -func`
- Command: `CGO_ENABLED=0 go test -count=1 -covermode=atomic -coverprofile=<temp-profile> ./...`
- Result: pass
- Overall: statements 85.8%
- Patch coverage: 90.0% of executable changed Go source lines (144 of 160); non-executable comments, imports and documentation are excluded.
- Baseline: v0.8.0 85.1% → HEAD 85.8% (delta +0.7%); no regression.
- Mutation score: n/a (no mutation tool configured and mutation was not requested).
- Test quality: 2.52 assertion tokens per top-level test function across 361 functions; 5 zero-assert wrappers/benchmarks reviewed as intentional; 0 tests over the 0.5s advisory threshold; slowest test 0.090s.
- Gate: threshold=80 (default, no repo hard coverage gate found); overall and patch coverage pass.

## Metrics
| Area | Statements | Notes |
|---|---:|---|
| `configkit` | 75.1% | advisory package-level warning |
| `fsutil` | 62.9% | advisory package-level warning |
| `sqlitekit` | 70.8% | advisory package-level warning |
| `embedkit` | 92.0% | new public package |
| `updatecheck/updateapply` | 79.8% | below default package average, above no hard gate |
| **Repository total** | **85.8%** | +0.7pp vs v0.8.0 |
| **Patch (executable changed lines)** | **90.0%** | 144 of 160 executable lines covered |

## Test quality
- **Mutation:** not run; no mutation tool is configured and the release sweep did not request mutation testing.
- **Assert density:** 911 assertion tokens across 361 top-level test functions, average 2.52. The five zero-assert tests are delegation wrappers around shared assertions or benchmark/smoke tests: `vectorkit/turboquant/codec_test.go:114-127` and `vectorkit/turboquant/benchmark_test.go:46`.
- **AAA:** no systemic issue identified from the static scan.
- **Performance:** 421 test events observed; slowest test 0.090s (`vectorkit/turboquant/TestRecallOnDeterministicFixture`); no test exceeded 0.5s.

## Findings
- [ ] **[Coverage/WARN] Targeted tests could improve lower-coverage utility paths**
  - **Status quo:** `fsutil` is 62.9% (`writeTempThenRename()`, `SafeWriteFile()`), `sqlitekit` is 70.8% (`Open()`, `Migrate()`), and `configkit` is 75.1% (`Reload()`, `setFieldValue()`). The repository has no hard coverage gate and total coverage is 85.8%.
  - **Likely cause:** filesystem, database, and reflection/error paths require more setup than the primary success paths.
  - **Proposed solution:** Add focused error-path tests for the named symbols in a future coverage pass; this is not a release blocker.
  - **Uncovered:** `fsutil/atomicwrite.go:12` (`writeTempThenRename()`), `fsutil/safewrite.go:15` (`SafeWriteFile()`), `sqlitekit/sqlitekit.go:26-64` (`Open()`/`Migrate()`), `configkit/configkit.go:92-104` (`Reload()`/`ResetCache()`), `configkit/configkit.go:304` (`setFieldValue()`)
  - **Effort/Impact:** Medium effort / medium impact

## Non-issues
- `updatecheck/updateapply.Reexec()` is process-replacing code and is not meaningfully covered by an in-process unit test.
- The five zero-assert tests delegate to shared assertion helpers or intentionally exercise benchmark/smoke behavior.

## Next step
Use this report as the coverage input for the prerelease gate. No coverage issue is release-blocking.
