<!-- prerelease: timestamp=2026-08-03T08:28:35Z repo=danieljustus/symaira-corekit head=8bef3327e89b59c8e617b37dbebe86bf21cdfdc3 target=v0.9.0 bump=minor gate=blocked -->

# Prerelease Gate — 2026-08-03

## Summary
- Repo: danieljustus/symaira-corekit
- HEAD: 8bef3327e89b59c8e617b37dbebe86bf21cdfdc3
- Last release: v0.8.0
- Proposed target: v0.9.0 (bump: minor)
- Gate: blocked

## Gate checks
| Check | Result | Detail |
|---|---|---|
| CI / tests (HEAD) | PASS | CI run 30762635860 and Push on main run 30762635652 completed successfully for the exact HEAD; `make build`, `make test`, `make lint`, and `go mod verify` pass locally. |
| Version consistency | PASS | The one current package-introduction mention in `README.md` was corrected from `v0.8.1` to `v0.9.0`; historical release references remain historical. |
| README / doc currency | BLOCK | `README.md` lists the new `embedkit` public API, but `docs/migrations.md` has no `v0.8.0 → v0.9.0` migration section describing the new package, provider, cache, dimension pinning, Ollama dimension support, and related consumer checks. |
| Coverage vs threshold | PASS | 85.8% statements; default threshold 80% because the repository defines no hard coverage gate. |
| Coverage regression | PASS | Baseline v0.8.0 85.1% → HEAD 85.8% (delta +0.7pp). |
| Patch coverage | PASS | 90.0% of executable changed Go source lines (144/160). |
| Assets preview | PASS | None expected: this is a Go library with no GoReleaser configuration or release workflow producing binaries. Repository branding images are documentation assets, not release artifacts. |
| Distribution publishers | PASS | No Homebrew Formula/Cask, registry publisher, Docker publisher, or release workflow is configured. |
| Security | PASS | 0 open Dependabot alerts and 0 open code-scanning alerts returned by GitHub. |

## Findings
- [ ] **[BLOCK][Docs] Add migration notes for the v0.9.0 public API additions**
  - **Status quo:** `docs/migrations.md` currently ends with `v0.7.0 → v0.8.0`, while `README.md` now advertises `embedkit` as introduced in `v0.9.0`. The unreleased diff adds `embedkit` (`Result`, `SameSpace`, providers, cache, dimension pinning), `ollamakit.EmbedWithDim`, consumer-drift documentation, and dependency/API changes.
  - **Likely cause:** The migration document was added before the current unreleased feature set and was not extended after the embedkit work landed.
  - **Proposed solution:** Add a `v0.8.0 → v0.9.0` section to `docs/migrations.md` with consumer-facing migration guidance and the release link. Re-run the prerelease gate afterward.
  - **Handoff:** `/02-gh-plan --from-review` → `/03-gh-go`; then re-run `/06-gh-prerelease`.

## Applied mechanical fixes
- `README.md:31` package introduction `v0.8.1` → `v0.9.0`

## Coverage input
- Report: `.github/coverage/2026-08-03T08-25-18Z.md`

## Next step
- gate=blocked → resolve the documentation finding with `/02-gh-plan --from-review` → `/03-gh-go`, then re-run `/06-gh-prerelease`.
